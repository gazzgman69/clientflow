Goal
Make Google OAuth run in a popup. On callback, notify the opener and close the popup instead of redirecting the popup back to the app.

Do not change DB schema or token logic.

1) Parent window: open popup + listen

In the unified email modal/button handler for Google:

function connectGoogleWithPopup() {
  const origin = window.location.origin;
  const w = window.open(`/auth/google/start?popup=1&origin=${encodeURIComponent(origin)}`,
    'oauth-google',
    'width=520,height=700,menubar=0,toolbar=0,status=0'
  );

  function onMsg(ev: MessageEvent) {
    if (ev.origin !== origin) return;
    if (ev.data?.type === 'oauth:connected' && ev.data?.provider === 'google') {
      window.removeEventListener('message', onMsg);
      try { w?.close(); } catch {}
      // refresh connected state (refetch accounts or reload)
      location.reload();
    }
  }
  window.addEventListener('message', onMsg);

  // safety: if the popup is blocked/closed, fall back
  const timer = setInterval(() => {
    if (!w || w.closed) { window.removeEventListener('message', onMsg); clearInterval(timer); }
  }, 500);
}


Wire the Google Connect button to connectGoogleWithPopup().

2) Start route: preserve popup info in state

Update /auth/google/start to include state with:

const state = {
  tenantId, userId,
  popup: req.query.popup === '1',
  origin: req.query.origin || `${req.protocol}://${req.get('host')}`,
  returnTo: '/settings/email-and-calendar'
};


generateAuthUrl({ state: Buffer.from(JSON.stringify(state)).toString('base64'), … }).

3) Callback route: postMessage + close

In /auth/google/callback, after you upsert the account, do not redirect when state.popup===true.
Instead, send a tiny HTML page that posts a message to the opener and closes the window:

if (parsed.popup) {
  const payload = {
    type: 'oauth:connected',
    provider: 'google',
    ok: true,
    accountEmail: email || null
  };
  const targetOrigin = parsed.origin; // validate this matches your app origin!

  res.setHeader('Content-Type', 'text/html; charset=utf-8');
  res.send(`<!doctype html><html><head><meta charset="utf-8">
  <title>Connecting…</title></head><body>
  <script>
    (function(){
      try {
        var data = ${JSON.stringify(payload)};
        window.opener && window.opener.postMessage(data, ${JSON.stringify(targetOrigin)});
      } catch (e) {}
      // close quickly; fallback redirect if blocked
      try { window.close(); } catch(e) {}
      setTimeout(function(){ location.replace(${JSON.stringify(parsed.returnTo || '/settings/email-and-calendar')}); }, 800);
    })();
  </script>
  </body></html>`);
  return;
}

// non-popup fallback (direct link flows): redirect as before
res.redirect(parsed.returnTo || '/settings/email-and-calendar');

4) Security checks

Before using parsed.origin, validate it equals your app origin (e.g., https://<your-replit-subdomain>.replit.dev). If it doesn’t, default to your app origin.

Never echo tokens to the page. Only send a minimal “connected” message.

5) Acceptance

Clicking Connect Google opens a popup to Google.

After consent, the popup does not load the CRM; it posts {type:'oauth:connected',provider:'google'} to the parent, closes itself, and the parent refreshes to show “connected”.

Microsoft flow can use the same pattern later (optional).